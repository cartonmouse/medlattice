# 项目规格：Qwen3 医疗领域问答系统

## 1. 项目定位

本项目用于模型工程实验，目标是构建一个可解释、可评测、可复现的中文医疗领域问答实验系统。它不是临床系统，也不用于真实医疗决策。

项目初始设想包含 Qwen3-1.7B、QLoRA、Faiss、DPO 和性能对比。本文件将这些内容拆成实验阶段；在实际测量前，初始设想中的数字全部视为待验证目标，不视为已完成结果。

## 2. 技术路线

```text
固定测试集
    │
    ├── Qwen3-1.7B 基线
    │
    ├── 数据清洗与 SFT/QLoRA
    │       └── 与基线对比
    │
    ├── 文档库与 Faiss RAG
    │       └── 与无检索版本对比
    │
    └── 偏好数据与 DPO
            └── 与 SFT 版本对比
```

## 3. 阶段与验收标准

### 阶段 A：基线推理

目标：使用固定数据格式运行 `Qwen/Qwen3-1.7B`，保存每条样本的生成结果和运行指标。

必须记录：

- 模型名称和模型版本/提交号
- Transformers、PyTorch、CUDA 版本
- GPU 型号和显存
- prompt 模板、是否开启 thinking、最大新 token 数
- 输入 token 数、输出 token 数、单条延迟、tokens/s
- 失败样本和异常信息

验收：同一环境、同一随机种子和同一配置能够重新生成同结构的结果文件。

### 阶段 A-2：固定 benchmark 与自动评测

目标：先使用可判定的选择题建立评测链路，避免在开放式答案上直接使用不透明的“准确率”。

当前 `benchmark_v0.jsonl` 只用于工程 smoke test，包含 12 条人工编写的医学术语选择题。评测器报告选项字母 exact match accuracy、invalid output rate 和逐题结果。正式数据集接入后，需要重新核验来源、许可证、数据划分和答案标准。

结果解释时需要说明：选择题 exact match 只能衡量答案选项是否匹配，不能证明模型具备临床可靠性；开放式医疗问答还需要更细的评分规则、引用检查和安全性评审。

### 阶段 B：数据工程

目标：为正式训练选择有明确来源和许可证的公开数据，完成清洗和划分。

当前候选是 `FreedomIntelligence/CMB` 的 `CMB-Exam` 配置。上游数据卡声明许可证为 Apache-2.0，并提供 train/val/test 划分；第一版只保留单项选择题，分别最多生成 5000 条训练样本、280 条验证样本和固定 1000 条测试 smoke subset。当前读取结果显示 train 有答案、val 有答案和解释、test 不公开答案，因此 val 用于有标签评测，test 只用于无标签推理或格式检查，不能报告 test accuracy。原始数据不提交到 GitHub，处理脚本和来源记录提交到仓库，具体规则见 `data/sources/cmb-exam.yaml`。

必须完成：

- 来源、许可证和用途说明
- 去重和异常样本处理
- 训练/验证/测试集划分规则
- 防止同一问题变体跨集合泄漏
- 医疗隐私和安全审查
- 上游版本、下载时间和本地处理结果 SHA-256

第一版不追求 35 万条数据，先使用小规模样本把流程跑通。

### 阶段 C：SFT/QLoRA

目标：在固定训练配置下训练 LoRA adapter，并与基线比较。

当前首版将 CMB 单项选择题转换为 Qwen `messages` 格式，assistant target 只使用参考答案字母，并用 4-bit NF4 + LoRA 做 smoke training。8 条样本的一步实验已经证明训练、保存、加载和推理链路可用，但不作为效果结论；正式验收必须使用完整固定子集并报告 base/adapter 的同条件对比。

需要记录：

- 量化类型、LoRA rank、alpha、dropout 和 target modules
- max sequence length、batch size、gradient accumulation
- 学习率、训练步数、warmup、随机种子
- 峰值显存、训练耗时和 checkpoint 大小
- 基线、微调后和不同配置的对比

本机 8GB 显存只作为小规模 smoke test 环境；如果正式配置需要更大显存，必须单独记录云 GPU 的型号、价格/时长和复现命令。

### 阶段 D：RAG

目标：构建文档切分、向量化、Faiss 检索和答案生成链路。

至少评测：

- Recall@K 或 Hit@K
- MRR 或 nDCG（如测试集适用）
- 检索 p50/p95 延迟
- 端到端 p50/p95 延迟
- 无 RAG、基础 Faiss、优化索引三组对比

“K-means 优化检索”不能直接作为结论。需要说明它是否用于 IVF 索引训练、聚类数量如何选择，以及延迟和召回率是否真的改善。

### 阶段 E：DPO

目标：在 SFT 稳定后，使用质量可解释的 chosen/rejected 偏好对进行对齐。

必须回答：

- 偏好数据由谁、按什么规则构造
- rejected 答案为什么更差
- reference model 如何选择
- beta、学习率和训练步数如何确定
- DPO 是否改善了正确性、格式遵循或安全性

如果偏好数据质量不足，宁可把 DPO 作为失败实验记录，也不为了凑技术栈强行写入项目记录。

### 阶段 F：服务化与开源

目标：提供可运行的推理接口和最小复现实验。

验收：

- 一条命令完成环境检查
- 一条命令运行基线或加载 adapter 推理
- Docker 或明确的本地启动方式
- 至少一组自动化测试
- README 中包含架构、数据许可、配置、结果和限制
- 不提交密钥、原始医疗数据和未经许可的模型文件

## 4. 指标口径

所有指标必须同时记录数值、数据集版本、样本数量、硬件、生成配置和代码版本。不能只写“准确率提升”，还要说明准确率的计算方式和评测脚本。

第一阶段只记录生成性能，不提前承诺初始设想中的 62%→78%、89% 召回率或 45 tokens/s。等实验完成后，再根据真实结果更新项目记录。
