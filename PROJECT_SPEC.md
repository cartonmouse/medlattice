# 项目规格：Qwen3 医疗领域问答系统

## 1. 项目定位

本项目用于学习和面试展示，目标是构建一个可解释、可评测、可复现的中文医疗领域问答实验系统。它不是临床系统，也不用于真实医疗决策。

简历中的原始设想包含 Qwen3-1.7B、QLoRA、Faiss、DPO 和性能对比。本文件将这些内容拆成实验阶段；在实际测量前，简历中的数字全部视为待验证目标，不视为已完成结果。

## 2. 技术路线

```text
固定测试集
    │
    ├── Qwen3-1.7B 基线
    │
    ├── 数据清洗与 SFT/QLoRA
    │       └── 与基线对比
    │
    ├── 文档库与 Faiss RAG
    │       └── 与无检索版本对比
    │
    └── 偏好数据与 DPO
            └── 与 SFT 版本对比
```

## 3. 阶段与验收标准

### 阶段 A：基线推理

目标：使用固定数据格式运行 `Qwen/Qwen3-1.7B`，保存每条样本的生成结果和运行指标。

必须记录：

- 模型名称和模型版本/提交号
- Transformers、PyTorch、CUDA 版本
- GPU 型号和显存
- prompt 模板、是否开启 thinking、最大新 token 数
- 输入 token 数、输出 token 数、单条延迟、tokens/s
- 失败样本和异常信息

验收：同一环境、同一随机种子和同一配置能够重新生成同结构的结果文件。

### 阶段 B：数据工程

目标：为正式训练选择有明确来源和许可证的公开数据，完成清洗和划分。

必须完成：

- 来源、许可证和用途说明
- 去重和异常样本处理
- 训练/验证/测试集划分规则
- 防止同一问题变体跨集合泄漏
- 医疗隐私和安全审查

第一版不追求 35 万条数据，先使用小规模样本把流程跑通。

### 阶段 C：SFT/QLoRA

目标：在固定训练配置下训练 LoRA adapter，并与基线比较。

需要记录：

- 量化类型、LoRA rank、alpha、dropout 和 target modules
- max sequence length、batch size、gradient accumulation
- 学习率、训练步数、warmup、随机种子
- 峰值显存、训练耗时和 checkpoint 大小
- 基线、微调后和不同配置的对比

本机 8GB 显存只作为小规模 smoke test 环境；如果正式配置需要更大显存，必须单独记录云 GPU 的型号、价格/时长和复现命令。

### 阶段 D：RAG

目标：构建文档切分、向量化、Faiss 检索和答案生成链路。

至少评测：

- Recall@K 或 Hit@K
- MRR 或 nDCG（如测试集适用）
- 检索 p50/p95 延迟
- 端到端 p50/p95 延迟
- 无 RAG、基础 Faiss、优化索引三组对比

“K-means 优化检索”不能直接作为结论。需要说明它是否用于 IVF 索引训练、聚类数量如何选择，以及延迟和召回率是否真的改善。

### 阶段 E：DPO

目标：在 SFT 稳定后，使用质量可解释的 chosen/rejected 偏好对进行对齐。

必须回答：

- 偏好数据由谁、按什么规则构造
- rejected 答案为什么更差
- reference model 如何选择
- beta、学习率和训练步数如何确定
- DPO 是否改善了正确性、格式遵循或安全性

如果偏好数据质量不足，宁可把 DPO 作为失败实验记录，也不为了凑技术栈强行写入简历。

### 阶段 F：服务化与开源

目标：提供可运行的推理接口和最小复现实验。

验收：

- 一条命令完成环境检查
- 一条命令运行基线或加载 adapter 推理
- Docker 或明确的本地启动方式
- 至少一组自动化测试
- README 中包含架构、数据许可、配置、结果和限制
- 不提交密钥、原始医疗数据和未经许可的模型文件

## 4. 指标口径

所有指标必须同时记录数值、数据集版本、样本数量、硬件、生成配置和代码版本。不能只写“准确率提升”，还要说明准确率的计算方式和评测脚本。

第一阶段只记录生成性能，不提前承诺简历中的 62%→78%、89% 召回率或 45 tokens/s。等实验完成后，再根据真实结果更新简历。

## 5. 面试叙事主线

可以按下面的顺序讲：

1. 通用模型在目标领域存在知识覆盖和格式稳定性问题。
2. 先建立固定测试集和基线，避免无法判断改动是否有效。
3. 用 QLoRA 低成本学习领域表达和任务格式。
4. 用 RAG 注入可更新、可追溯的文档知识，减少把所有知识写入参数的需要。
5. 在 SFT 稳定后用 DPO 调整答案偏好，但通过对比实验确认它是否真的有收益。
6. 最后把实验封装成服务，并用日志、测试和配置保证复现。

