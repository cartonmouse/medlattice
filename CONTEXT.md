# Qwen 医疗问答实验系统

本上下文定义项目在实验、评测和面试叙事中使用的领域语言。它明确区分模型工程实验与临床系统，避免把合成数据结果或选择题指标误称为医疗能力或临床安全结论。

## 项目边界

**医疗问答实验系统**：用于验证中文医疗问答模型工程链路的可复现研究项目。
_Avoid_: 临床系统、诊疗系统、医疗决策系统

**临床安全结论**：需要真实场景、领域专家或医生标注、合法数据、人工评审和更严格验证后才能讨论的结论。本项目不声称已经获得该结论。
_Avoid_: 医疗安全率、临床准确率、医生级能力

## 任务与评测

**CMB-Exam 选择题基准**：用于固定模型输入、输出口径和隔离评测的中文单项选择题集合。其主要指标是选项字母 exact-match，不等于开放式医疗问答质量。
_Avoid_: 医疗知识准确率、临床 benchmark

**评测闭环**：从固定数据划分、基线运行、实验对照、指标记录到失败样本分析的完整过程。
_Avoid_: 只看一个最终准确率、上线效果

**合成安全基准**：由工程规则设计的安全场景、变体和期望标签组成的测试集，用于发现检测和修订问题，不等同于专家安全标注。
_Avoid_: 临床安全测试集、医生金标准

## 知识注入与训练

**闭域例题 RAG**：只从 CMB-Exam train split 构建已解答例题检索库，并在隔离 val 上观察检索对选择题输出的影响。它验证的是闭域例题检索链路，不是通用医学知识库问答。
_Avoid_: 医学知识 RAG、临床知识库

**生成式 RAG**：先检索上下文，再让模型依据上下文生成回答并提供引用编号的推理链路。当前合成演示中的 grounded 指标属于自动化代理指标。
_Avoid_: 引用忠实性证明、事实性证明

**训练适配器**：通过 QLoRA/SFT 得到的轻量参数增量，用于改变模型在特定任务上的输出行为；它不是完整基础模型，也不自动代表领域知识已经可靠写入参数。
_Avoid_: 重新训练后的完整模型、医疗大模型

**偏好优化实验**：对 DPO、hard-negative DPO 和 GRPO 等训练目标进行可审计对照的实验集合。当前偏好对或奖励主要是工程构造，不能默认解释为人类或临床专家偏好。
_Avoid_: 人类对齐完成、临床偏好对齐

## 安全行为

**安全规则门禁**：对生成候选回答执行确定性原则检查；不通过时使用保守修订或拒答模板。它提供的是项目规则范围内的 fail-closed 约束，不是独立的医疗安全模型。
_Avoid_: 临床安全护栏、绝对安全保证

**模型独立修订**：只观察 Qwen critic/revision 输出是否通过规则的指标，用来衡量模型自身修订能力。
_Avoid_: 最终安全率

**最终规则通过**：模型修订经过确定性安全规则和必要兜底后的结果。该指标必须与模型独立修订成功率、fallback 数量和基准类型一起报告。
_Avoid_: 模型本身达到 100% 安全

**资料不足拒答**：当问题超出项目范围、缺少支持上下文或涉及高风险个人处置时，明确表达无法判断并给出合适的专业求助方向。
_Avoid_: 泛化拒答、无理由拒答
